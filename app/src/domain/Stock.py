class Stock():
    def __init__(self, stock_id, ticker, company_name, status, current_price):
        self.stock_id = stock_id
        self.ticker = ticker 
        self.company_name = company_name
        self.status = status 
        self.current_price = current_price
